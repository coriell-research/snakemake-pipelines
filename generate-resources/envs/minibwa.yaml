channels:
  - bioconda
  - conda-forge
dependencies:
  # matches the version already installed system-wide at
  # /usr/local/programs/minibwa (0.7-r424), which is also bioconda's newest.
  - minibwa=0.7
